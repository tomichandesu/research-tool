# 中国製品リサーチシステム 包括的技術調査レポート

**調査日**: 2026-02-02
**目的**: コストゼロで Amazon-1688 画像マッチング・利益計算システムを構築

---

## エグゼクティブサマリー

### 🏆 推奨アプローチ: **無料（手動BSR確認）方式**

| 比較項目 | Keepa API（有料） | 手動BSR確認（無料） |
|---------|:----------------:|:------------------:|
| **コスト** | €19/月（約3,000円） | **¥0** |
| **BSR取得** | 自動・履歴あり | 手動・現在値のみ |
| **処理速度** | 高速（一括取得） | 低速（1件ずつ） |
| **実装難易度** | 容易 | やや複雑 |
| **精度** | 高（履歴データ） | 中（スナップショット） |

**結論**: コストゼロを優先するなら **手動BSR確認方式** を採用し、Playwrightで商品詳細ページを自動巡回する

---

## 1. BSR（売上ランキング）取得方法の比較

### 1.1 Keepa API（有料）

| 項目 | 内容 |
|------|------|
| 料金 | €19/月（約3,000円） |
| 機能 | BSR履歴、価格履歴、販売数推定 |
| メリット | 一括取得、履歴データ、高精度 |
| デメリット | **有料** |

**出典**: [Keepa Alternatives](https://tracefuse.ai/blog/best-keepa-alternatives/)

### 1.2 手動BSR確認（無料）- 推奨

Playwrightで商品詳細ページを自動巡回し、BSRを取得する方式。

```python
# 商品詳細ページからBSRを取得
async def get_bsr_from_product_page(page, asin):
    url = f"https://www.amazon.co.jp/dp/{asin}"
    await page.goto(url)

    # BSRは「登録情報」セクションにある
    bsr_section = await page.query_selector("#productDetails_detailBullets_sections1")
    bsr_text = await bsr_section.inner_text()

    # 「Amazon 売れ筋ランキング: XX位」を抽出
    import re
    match = re.search(r'売れ筋ランキング[:\s]+(\d+)', bsr_text.replace(',', ''))
    if match:
        return int(match.group(1))
    return None
```

**メリット**:
- 完全無料
- 現在のBSRを正確に取得

**デメリット**:
- 1件あたり2-3秒かかる
- 履歴データなし

**出典**: [Oxylabs - Scrape Amazon Best Sellers](https://oxylabs.io/blog/how-to-scrape-amazon-best-sellers)

### 1.3 無料代替ツール

| ツール | 機能 | 制限 |
|--------|------|------|
| CamelCamelCamel | 価格履歴（無料） | BSRなし、APIなし |
| Helium 10 Free | BSR確認（1000回/月） | 月間制限あり |
| 在庫追跡法 | 販売数を直接計測 | 手動・時間がかかる |

**出典**: [CamelCamelCamel vs Keepa](https://goaura.com/blog/camelcamelcamel-vs-keepa)

---

## 2. BSRから販売数を推定する方法

### 2.1 大カテゴリー別 月間販売数の目安

| ランキング | 月間販売数（目安） |
|------------|-------------------|
| 1位〜1,000位 | 1,000個〜数千個 |
| 1,000位〜3,000位 | 300〜900個 |
| 3,000位〜5,000位 | 100〜300個 |
| 5,000位〜10,000位 | 50〜100個 |
| 10,000位〜20,000位 | 約20個 |
| 20,000位〜30,000位 | 1〜20個 |

**重要**: カテゴリーによって大きく異なる。「ドラッグストア」1,000位と「ベビー」1,000位では10倍以上の差がある場合も。

**出典**: [Amazonランキングの調べ方](https://bxo.co.jp/magazine/2355)

### 2.2 あなたの条件に照らし合わせると

| 条件 | BSR目安 |
|------|---------|
| FBA: 月売上2万円以上（価格1,500円の場合 = 14個以上） | 約15,000位以内 |
| FBM: 月3個以上 | 約30,000位以内 |

### 2.3 計算式（概算）

```python
def estimate_monthly_sales(bsr: int, category: str = "default") -> int:
    """
    BSRから月間販売数を推定（無料の概算式）
    """
    # カテゴリー別係数（経験値）
    coefficients = {
        "home_kitchen": (5000, 0.75),    # ホーム&キッチン
        "toys": (3500, 0.80),            # おもちゃ
        "beauty": (8000, 0.70),          # ビューティー
        "electronics": (2500, 0.85),     # 家電
        "default": (4000, 0.78)
    }

    a, b = coefficients.get(category, coefficients["default"])
    monthly_sales = int(a * (bsr ** (-b)) * 30)

    return max(1, monthly_sales)
```

**出典**: [寺田式物販スクール](https://teradamasanobu.com/amazonsell/post-4932/)

---

## 3. システムアーキテクチャ（コストゼロ版）

### 3.1 全体フロー

```
┌─────────────────────────────────────────────────────────────┐
│ ユーザー入力: キーワード（例：「貯金箱」）                   │
└─────────────────────┬───────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Amazon検索（Playwright）                            │
│   - キーワードで検索                                        │
│   - 商品一覧を取得（ASIN、タイトル、価格、画像URL）         │
└─────────────────────┬───────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: 基本フィルタリング                                  │
│   - 価格 ≥ 1,500円                                          │
│   - 検索結果から絞り込み                                    │
└─────────────────────┬───────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: 商品詳細ページ巡回（Playwright）                    │
│   - 各商品ページにアクセス                                  │
│   - BSR（大カテゴリーランキング）を取得                     │
│   - レビュー数を取得                                        │
│   - FBA/FBM判定                                             │
└─────────────────────┬───────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 4: 条件フィルタリング                                  │
│   - レビュー ≤ 40                                           │
│   - BSRから販売数推定 → 条件判定                            │
│     - FBA: 月売上2万円以上                                  │
│     - FBM: 月3個以上                                        │
└─────────────────────┬───────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 5: 1688画像検索（Playwright）                          │
│   - Amazon商品画像URLで1688を検索                           │
│   - 検索結果から商品リストを取得                            │
└─────────────────────┬───────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 6: 画像完全一致判定（pHash）                           │
│   - Amazon画像と1688画像のハッシュを比較                    │
│   - 差分 < 5 で完全一致と判定                               │
└─────────────────────┬───────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 7: 利益計算                                            │
│   - Amazon価格 - (1688価格 + 送料 + 関税 + 手数料)          │
│   - 利益率・利幅を計算                                      │
└─────────────────────┬───────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 8: 結果出力（CSV）                                     │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 処理時間の見積もり

| ステップ | 処理時間/件 | 100件の場合 |
|---------|------------|-------------|
| Amazon検索 | 3秒 | 3秒（1回） |
| 商品詳細ページ | 2秒 | **200秒（約3分）** |
| 1688画像検索 | 3秒 | 300秒（約5分） |
| 画像ハッシュ | 0.1秒 | 10秒 |
| **合計** | - | **約10分** |

---

## 4. 必要なツール・ライブラリ（すべて無料）

### 4.1 Pythonライブラリ

```bash
pip install playwright imagehash pillow requests beautifulsoup4
playwright install chromium
```

| ライブラリ | 用途 | コスト |
|-----------|------|--------|
| Playwright | ブラウザ自動化 | 無料 |
| imagehash | 画像ハッシュ（pHash） | 無料 |
| Pillow | 画像処理 | 無料 |
| requests | HTTP通信 | 無料 |
| BeautifulSoup | HTMLパース | 無料 |

### 4.2 MCP（Model Context Protocol）サーバー

| MCP | 用途 | 出典 |
|-----|------|------|
| **Playwright MCP** | ブラウザ自動化 | [Microsoft Playwright MCP](https://github.com/microsoft/playwright-mcp) |
| **Amazon MCP** | 商品検索（既存接続済み） | [MCPMarket](https://mcpmarket.com/server/amazon-shopping) |

**Playwright MCP の設定例**:

```json
// .claude/settings.json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"]
    }
  }
}
```

**出典**: [Simon Willison - Playwright MCP](https://til.simonwillison.net/claude-code/playwright-mcp-claude-code)

### 4.3 SkillsMP で使えるスキル

| スキル | 用途 |
|--------|------|
| web-scraping | Webスクレイピング全般 |
| firecrawl | Webコンテンツ取得 |
| deep-research | 自律的リサーチ |

**出典**: [SkillsMP](https://skillsmp.com/)

---

## 5. 画像完全一致判定（pHash）

### 5.1 閾値の設定

| 差分（ビット数） | 判定 |
|-----------------|------|
| 0 | **完全一致** |
| 1-2 | ほぼ同一（微細な違い） |
| 3-5 | 類似（加工・リサイズ等） |
| 6-10 | 似ている可能性 |
| >10 | 異なる画像 |

### 5.2 実装コード

```python
from imagehash import phash
from PIL import Image
import requests
from io import BytesIO

def download_image(url: str) -> Image:
    """URLから画像をダウンロード"""
    response = requests.get(url, timeout=10)
    return Image.open(BytesIO(response.content))

def is_image_match(amazon_url: str, alibaba_url: str, threshold: int = 5) -> bool:
    """
    2つの画像が完全一致かどうかを判定

    Args:
        amazon_url: Amazon商品画像URL
        alibaba_url: 1688商品画像URL
        threshold: 差分の閾値（デフォルト5）

    Returns:
        True: 完全一致
        False: 不一致
    """
    try:
        img1 = download_image(amazon_url)
        img2 = download_image(alibaba_url)

        hash1 = phash(img1)
        hash2 = phash(img2)

        difference = hash1 - hash2

        return difference <= threshold
    except Exception as e:
        print(f"Error comparing images: {e}")
        return False
```

**出典**: [PyImageSearch - Image Hashing](https://pyimagesearch.com/2017/11/27/image-hashing-opencv-python/)

---

## 6. 1688画像検索の自動化

### 6.1 方法1: Playwrightで直接操作（推奨）

```python
async def search_1688_by_image(page, image_url: str) -> list:
    """
    1688で画像検索を実行
    """
    # 1688の画像検索ページ
    await page.goto("https://s.1688.com/youyuan/index.htm")

    # 画像URL入力
    await page.fill("input.search-input", image_url)
    await page.click("button.search-btn")

    # 結果を待機
    await page.wait_for_selector(".offer-list-row", timeout=10000)

    # 検索結果を取得
    items = await page.query_selector_all(".offer-list-row .offer-item")

    results = []
    for item in items[:10]:  # 上位10件
        try:
            price_el = await item.query_selector(".price")
            img_el = await item.query_selector("img")
            link_el = await item.query_selector("a")

            results.append({
                "price_cny": await price_el.inner_text() if price_el else None,
                "image_url": await img_el.get_attribute("src") if img_el else None,
                "product_url": await link_el.get_attribute("href") if link_el else None
            })
        except:
            continue

    return results
```

### 6.2 方法2: Chrome拡張機能を活用

| 拡張機能 | 特徴 |
|---------|------|
| 1688 Search by Image | ワンクリックで画像検索 |
| AliPrice | 価格履歴追跡機能付き |

**出典**: [1688 Search by Image](https://chromewebstore.google.com/detail/1688-search-by-image/dbjldigbjceebginhcnmjnigbocicokh)

---

## 7. 利益計算ロジック

### 7.1 計算式

```
利益 = Amazon販売価格 - 総コスト

総コスト = 1688仕入価格(円換算)
         + 国際送料
         + 関税
         + Amazon紹介料
         + FBA手数料（FBAの場合）
```

### 7.2 実装コード

```python
def calculate_profit(
    amazon_price: int,
    cny_price: float,
    weight_kg: float = 0.5,
    is_fba: bool = True,
    category: str = "home"
) -> dict:
    """
    利益を計算

    Args:
        amazon_price: Amazon販売価格（円）
        cny_price: 1688仕入価格（元）
        weight_kg: 商品重量（kg）
        is_fba: FBAかどうか
        category: 商品カテゴリ

    Returns:
        利益情報の辞書
    """
    # 為替レート（2026年想定）
    JPY_PER_CNY = 21.5

    # 1688価格（円換算）
    cost_jpy = cny_price * JPY_PER_CNY

    # 国際送料（重量ベース概算）
    # 一般的な代行業者の送料: 約1,200-1,500円/kg
    shipping_jpy = weight_kg * 1300

    # 関税（簡易税率: 約5-10%、1万円以下は免税の場合あり）
    import_cost = cost_jpy + shipping_jpy
    if import_cost > 10000:
        customs_duty = import_cost * 0.05  # 5%概算
    else:
        customs_duty = 0

    # Amazon紹介料（カテゴリ別: 8-15%）
    referral_fees = {
        "home": 0.15,
        "toys": 0.15,
        "beauty": 0.10,
        "electronics": 0.08,
        "default": 0.15
    }
    referral_rate = referral_fees.get(category, 0.15)
    amazon_referral_fee = amazon_price * referral_rate

    # FBA手数料（サイズ・重量別）
    if is_fba:
        if amazon_price < 1000:
            fba_fee = 290
        elif amazon_price < 2000:
            fba_fee = 420
        else:
            fba_fee = 530
    else:
        fba_fee = 0

    # 総コスト
    total_cost = cost_jpy + shipping_jpy + customs_duty + amazon_referral_fee + fba_fee

    # 利益
    profit = amazon_price - total_cost
    profit_rate = (profit / amazon_price) * 100 if amazon_price > 0 else 0

    return {
        "amazon_price": amazon_price,
        "cost_1688_jpy": int(cost_jpy),
        "shipping": int(shipping_jpy),
        "customs": int(customs_duty),
        "referral_fee": int(amazon_referral_fee),
        "fba_fee": fba_fee,
        "total_cost": int(total_cost),
        "profit": int(profit),
        "profit_rate": round(profit_rate, 1),
        "is_profitable": profit > 0
    }
```

---

## 8. MCPマーケット・スキルマーケット活用提案

### 8.1 MCPMarket.com で使えるMCP

| MCP | 用途 | URL |
|-----|------|-----|
| **Playwright MCP** | ブラウザ自動化 | [Microsoft](https://github.com/microsoft/playwright-mcp) |
| **Amazon Shopping** | 商品検索 | [MCPMarket](https://mcpmarket.com/server/amazon-shopping) |
| **Amazon Marketplace Data** | セラーデータ | [MCPMarket](https://mcpmarket.com/server/amazon-marketplace) |

### 8.2 SkillsMP.com で使えるスキル

| スキル | 用途 |
|--------|------|
| web-scraping | Webスクレイピング |
| firecrawl | Webコンテンツ取得 |
| deep-research | 自律リサーチ |
| image-analysis | 画像分析 |

### 8.3 推奨構成

```
Claude Code
├── MCP: Playwright MCP（ブラウザ自動化）
├── MCP: Amazon Marketplace（既存接続済み）
├── Skill: web-scraping（スクレイピング補助）
└── Python Scripts
    ├── amazon_scraper.py（Amazon巡回）
    ├── alibaba_search.py（1688画像検索）
    ├── image_matcher.py（pHash比較）
    └── profit_calculator.py（利益計算）
```

---

## 9. Keepa API vs 手動BSR の最終比較

### 9.1 処理効率

| 項目 | Keepa API | 手動BSR |
|------|-----------|---------|
| 100件処理 | 約30秒 | 約3分 |
| 1000件処理 | 約5分 | 約30分 |
| API制限 | あり | なし |

### 9.2 取得できるデータ

| データ | Keepa API | 手動BSR |
|--------|-----------|---------|
| 現在BSR | ✅ | ✅ |
| BSR履歴 | ✅ | ❌ |
| 価格履歴 | ✅ | ❌ |
| 販売数推定 | ✅（高精度） | △（概算） |

### 9.3 推奨

| シナリオ | 推奨方式 |
|---------|---------|
| コストゼロ最優先 | **手動BSR** |
| 精度・効率重視 | Keepa API |
| 月100件以下 | 手動BSR |
| 月1000件以上 | Keepa API |

---

## 10. 実装ロードマップ

### Phase 1: 基盤構築（1-2日）
- [ ] Playwright環境セットアップ
- [ ] Playwright MCPの設定
- [ ] 基本的なスクレイピングテスト

### Phase 2: Amazon巡回（2-3日）
- [ ] キーワード検索機能
- [ ] 商品詳細ページからBSR取得
- [ ] フィルタリングロジック

### Phase 3: 1688検索（2-3日）
- [ ] 1688画像検索の自動化
- [ ] 検索結果パース
- [ ] pHash比較実装

### Phase 4: 利益計算・出力（1-2日）
- [ ] 利益計算ロジック
- [ ] CSV出力
- [ ] レポート生成

### Phase 5: 統合テスト（1日）
- [ ] E2Eテスト
- [ ] パフォーマンス最適化

**合計: 約1-2週間**

---

## 出典一覧

1. [Keepa Alternatives - TraceFuse](https://tracefuse.ai/blog/best-keepa-alternatives/)
2. [Free Keepa Alternative for BSR - Ryan Hogue](https://ryanhogue.com/amazon-fba/free-keepa-alternative-for-tracking-bsr/)
3. [CamelCamelCamel vs Keepa - GoAura](https://goaura.com/blog/camelcamelcamel-vs-keepa)
4. [Scrape Amazon Best Sellers - Oxylabs](https://oxylabs.io/blog/how-to-scrape-amazon-best-sellers)
5. [Amazonランキングの調べ方 - ECの相談室](https://bxo.co.jp/magazine/2355)
6. [Amazon販売数予測 - 寺田式物販スクール](https://teradamasanobu.com/amazonsell/post-4932/)
7. [Playwright MCP - Microsoft](https://github.com/microsoft/playwright-mcp)
8. [Playwright MCP Claude Code - Simon Willison](https://til.simonwillison.net/claude-code/playwright-mcp-claude-code)
9. [Image Hashing - PyImageSearch](https://pyimagesearch.com/2017/11/27/image-hashing-opencv-python/)
10. [1688 Search by Image - Chrome Web Store](https://chromewebstore.google.com/detail/1688-search-by-image/dbjldigbjceebginhcnmjnigbocicokh)
11. [MCPMarket - Amazon Shopping](https://mcpmarket.com/server/amazon-shopping)
12. [SkillsMP](https://skillsmp.com/)
13. [ImageHash - PyPI](https://pypi.org/project/ImageHash/)
14. [AliPrice 1688 Search](https://www.aliprice.com/information/alibabaCnInformation.html)
15. [Sourcing Nova - 1688 Guide](https://sourcingnova.com/blog/1688-com-sourcing-guide/)
